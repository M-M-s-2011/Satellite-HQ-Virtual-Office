// Back end listeners for text chat

// add join chat and leave chat options to (1) add/remove socket for user that joins/leaves
// (2) notify the room when a user joins/leaves
// (This is distinguished from the user connecting to/disconnecting from the whole io channel)

module.exports = (io) => {};
