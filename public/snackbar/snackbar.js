const vidButton = document.getElementById("joinVid");
const chatButton = document.getElementById("joinChat");

vidButton.addEventListener("click", () => {
  console.log("joining video!");
});

chatButton.addEventListener("click", () => {
  console.log("joining chat!");
});
